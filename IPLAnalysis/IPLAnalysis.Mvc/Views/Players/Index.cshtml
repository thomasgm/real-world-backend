@model IEnumerable<IPLAnalysis.Mvc.Models.Player>

@{
    ViewData["Title"] = "IPL Players Analysis";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-4 fw-bold text-primary">IPL Player Insights</h1>
        <div>
            <a asp-action="TeamSpending" class="btn btn-outline-primary me-2">Team Spending</a>
            <a asp-action="TopEarners" class="btn btn-outline-success me-2">Top Earners</a>
            <a asp-action="RoleStats" class="btn btn-outline-info">Role Stats</a>
            <a asp-action="TopAllRounders" class="btn btn-outline-warning">Top All-Rounders</a>
            <a asp-action="HighestPricedPerTeam" class="btn btn-outline-danger ms-2">Highest Priced per Team</a>
            <a asp-action="Top2PerTeam" class="btn btn-outline-info ms-2">Top 2 per Team</a>
            <a asp-action="MostAndSecondMostExpensivePerTeam" class="btn btn-outline-secondary ms-2">Most & 2nd Most Expensive per Team</a>
            <a asp-action="ContributionPercentage" class="btn btn-outline-dark ms-2">Contribution %</a>
            <a asp-action="PriceCategoryDistribution" class="btn btn-outline-primary ms-2">Price Category Distribution</a>
            <a asp-action="TypeComparison" class="btn btn-outline-success ms-2">Type Comparison</a>
            <a asp-action="AboveAveragePlayers" class="btn btn-outline-info ms-2">Above Average Players</a>
            <a asp-action="TopPlayersByRole" class="btn btn-outline-warning ms-2">Role-wise Price Distribution</a>
        </div>
    </div>

    <div class="card shadow-sm border-0 bg-light">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-dark">
                        <tr>
                            <th>Name</th>
                            <th>Team</th>
                            <th>Role</th>
                            <th>Type</th>
                            <th>Price (Cr)</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var player in Model)
                        {
                            <tr>
                                <td class="fw-bold">@player.PlayerName</td>
                                <td><span class="badge bg-secondary">@player.Team</span></td>
                                <td>@player.Role</td>
                                <td>@player.Type</td>
                                <td class="text-success fw-bold">₹ @player.Price_in_cr Cr</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
